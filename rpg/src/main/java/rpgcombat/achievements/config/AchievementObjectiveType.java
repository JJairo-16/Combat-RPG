package rpgcombat.achievements.config;

/** Tipus d'objectiu que pot observar el motor d'assoliments. */
public enum AchievementObjectiveType {
    COUNT_EVENT,
    SUM_VALUE,
    CONDITIONAL_COUNT,
    CONDITIONAL_SUM,
    CONSECUTIVE_EVENT,
    AVOID_EVENT_FOR_TURNS,
    ACTION_SEQUENCE,
    STATE_REACHED,
    STATE_MAINTAINED,
    ALL_UNIQUE_VALUES,
    EACH_UNIQUE_VALUE_COUNT,
    MAX_VALUE_REACHED
}
